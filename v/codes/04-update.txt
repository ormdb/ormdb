var sql = Odb.SQL.Update(x =>
                x.From(th)
                    .Where(th.Year == 2018)
                    .SetRaw(th.IsActive, !th.IsActive)
                    .SetNull(th.Note)
                    .SetRaw(th.Id, th.Id + 1)
            );

sql.ExecuteNonQuery(cn);


