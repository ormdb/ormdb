var sql = Odb.SQL.Select(x => 
				x.From(th)
				.LeftJoin(tl, tl.Year == th.Year & 
								tl.Id == th.Id ) 
				.Where( th.Year > 2018 )
			);	

// reading data
var result = sql.ExecuteReader<TableHeader>(cn);


