//var cn = your connection

var sql = Odb.SQL.Insert();

var model = new Orders();
    model.CustomerID = 90;
    model.EmployeeID = 8;
    model.OrderDate = DateTime.Now;
    model.ShipperID = 1;

sql.Add(model);

var affectedRows = sql.ExecuteNonQuery(cn);