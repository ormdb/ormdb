var sql = Odb.SQL.Insert(true);
for (short n = 0; n < 100; n++)
{
    var model = new TableHeader();
    model.Year = DateTime.Now.Year;
    model.Id = n;
    model.DocDate = DateTime.Now.AddDays(1);

    sql.Add(model);
}

sql.ExecuteNonQuery(cn);